div(class="card")
  div(class="card-header")
    = part.name
  div(class="card-body")
    div(class="btn-group-vertical")
      form(action=url + "/action",method="post")
        each action in part.actions
          button(class="btn btn-secondary", name="action", value=action)= action